# Licenses

## tcxDepthRecord

MIT License — © tettou771

Header-only. Compression uses TrussC core's `tc::compress` (vendored lz4, BSD).
Bundles no third-party code of its own. Depends on `tcxDepthCamera` (MIT).
