# tcxFlowTools handoff - 2026-05-13

Workspace: `/Users/mac/Desktop/TrussC`

## Pause Context

- Active branch: `showlab`.
- User is pausing for a few days and wants the current state committed and pushed.
- Keep generated `build-macos` / `bin` app outputs in local example folders for manual viewing, but do not stage or commit those generated artifacts.
- Reference screenshots/videos are kept outside the repo under `/tmp/tcxFlowTools-reference-captures` and must not be committed.

## Reference Targets

The visual target is not API-name parity only. Each example should match the core visual effect of the reference implementation.

Reference pages/videos to use again on resume:

- ofxFlowTools visual reference: `https://vimeo.com/92334462`
- PixelFlow index/reference videos: `https://vimeo.com/diwi`
- Local PixelFlow reference code: `_fcache/PixelFlow/examples`
- Local ofxFlowTools reference code: `_fcache/ofxFlowTools-master` and `_fcache/ofxFlowTools-HD`

Known useful PixelFlow video IDs:

- Computational Fluid Dynamics:
  - WindTunnel: `https://vimeo.com/184850259`
  - StreamLines: `https://vimeo.com/184850254`
  - Verlet Particle Collision System: `https://vimeo.com/184849960`
  - Fluid Particles: `https://vimeo.com/184849959`
  - Liquid Painting - M.C. Escher: `https://vimeo.com/184849892`
  - Liquid Text: `https://vimeo.com/184849880`
- Softbody Dynamics:
  - SoftBody Dynamics 2D Playground: `https://vimeo.com/184853892`
  - SoftBody Dynamics 3D Cloth Simulation: `https://vimeo.com/184854746`
  - SoftBody Dynamics 3D Playground/Cloth: `https://vimeo.com/184854758`

Reference capture workflow that worked:

```bash
mkdir -p /tmp/tcxFlowTools-reference-captures/pixelflow/<example-name>
yt-dlp -f 'bv*[height<=360]+ba/b[height<=360]/worst' --merge-output-format mp4 \
  -o '/tmp/pixelflow-<example-name>.%(ext)s' https://vimeo.com/<id>
ffmpeg -y -i /tmp/pixelflow-<example-name>.mp4 \
  -vf "fps=1/3,scale=960:-1" -frames:v 5 \
  /tmp/tcxFlowTools-reference-captures/pixelflow/<example-name>/frame-%03d.png
```

Use the reference frames before implementing or tuning each example. Do not rely on memory or names alone.

## Completed And Accepted / Good Enough

### Bridge Texture Parity

- `VelocityBridge`, `DensityBridge`, `TemperatureBridge`, and `CombinedBridge` accept external GPU texture input and apply output textures to `Fluid2D`.
- `example-fluid-bridges` modes `1`, `2`, `3`, and `4` are no longer black/frozen. User confirmed mode behavior after screenshots.

### LIC / Streamlines First Pass

- `Fluid2D::drawLic()` and `example-lic-streamlines` exist.
- `l` toggles LIC, `d` toggles density.
- User screenshots showed both density-only and LIC views looking acceptable as a first pass.
- This is not full PixelFlow streamline particle rendering yet.

### Particle Variants First Pass

- `example-particle-variants` has flow / attractor / impulse modes.
- User noted modes `2` and `3` seem to show gather/scatter after clicking; code currently uses a moving `variantCenter`, not mouse-driven input. The first click is likely window focus plus the visible mode update.
- User said this and other already checked examples looked mostly correct.
- Remaining variants still missing: cohesion, dam-break, sprite trails, optical-flow capture particles.

### HD Output Split

- `FluidSettings::outputResolutionScale` and `example-hd` support separate simulation/output resolution.
- Exact ofxFlowTools-HD shader-family parity remains a tracked follow-up.

### Split Velocity First Pass

- `SplitVelocity` helper and `example-split-velocity` exist.
- User screenshot showed combined split-velocity output looking acceptable.
- Remaining ofx helper gaps are still tracked below.

### PixelFlow Liquid Text

- `example-fluid-liquid-text` exists and builds.
- User screenshot showed readable text density/temperature fluid behavior.

### PixelFlow Liquid Painting

- `example-fluid-liquid-painting` exists and was pushed in commit `d78c7d2`.
- It loads `_fcache/PixelFlow/examples/data/mc_escher.jpg` and injects the image into GPU density.
- User confirmed the final effect is correct after fixing the source image disappearing.
- Important implementation note: default `TCX_LIQUID_PAINTING_MIX=0.46` is intentional because the current GPU density texture path uses a max floor, not PixelFlow's tiny additive mix.

### PixelFlow SoftBody2D Cloth

- Independent `tcx::flow::SoftBody2D` exists inside `tcxFlowTools`.
- `example-softbody2d-cloth` exists.
- It does not depend on `tcxTraerPhysics`.
- User confirmed the visual effect is correct.

## Current WIP: Fluid Verlet Collision

New WIP example:

- `addons/tcxFlowTools/examples/example-fluid-verlet-collision`
- Based on PixelFlow `Fluid_VerletParticleCollisionSystem`.
- Build passes and the app exists at:
  - `addons/tcxFlowTools/examples/example-fluid-verlet-collision/bin/example-fluid-verlet-collision.app`

Current user feedback:

- Keep this example as-is for now and revisit after the pause.
- It is not visually correct yet.
- Problems observed:
  - Particles behave like left/right wave piles or bottom sand piles instead of PixelFlow's broad collision particle field.
  - Earlier attempts either made particles fall to the bottom, fly randomly, or stick in suspended clumps.
  - Mouse fluid interaction is still not correct: it appears as discontinuous circular dots rather than a smooth fluid streak.
  - Mouse interaction with particles is not strong/clear enough.

Reference frame already captured:

- `/tmp/tcxFlowTools-reference-captures/pixelflow/verlet-collision/frame-004.png`
- Reference video: `https://vimeo.com/184849960`
- The reference visual shows black/gray fluid smoke plus orange/blue/gray circular particles, dense collision, boundaries, vertical obstacle columns, and particle motion driven by fluid velocity.

Recommended next approach:

- Stop blind parameter tuning.
- Re-read PixelFlow:
  - `_fcache/PixelFlow/examples/Fluid2D/Fluid_VerletParticleCollisionSystem/Fluid_VerletParticleCollisionSystem.java`
  - `_fcache/PixelFlow/examples/Fluid2D/Fluid_VerletParticleCollisionSystem/ParticleSystem.java`
- Implement the particle side closer to the reference:
  - random full-screen particle distribution with no artificial home-position constraint;
  - low gravity and damping like PixelFlow;
  - collision iterations similar to PixelFlow's `iterations_collisions = 4`;
  - spatial partitioning or a more stable broad-phase instead of naive all-pairs tuning if needed;
  - particle force should be driven by sampled fluid velocity. Current `Fluid2D::sampleVelocityAtPosition()` samples CPU fallback arrays, so a real GPU velocity readback or a mirrored procedural force field is needed for visual parity.
- Fix mouse fluid separately:
  - current per-splat density creates dotted circles;
  - use a stroke/line-based injection texture or a denser interpolated FBO stroke, then apply that texture to density/velocity;
  - left mouse should visibly affect both fluid and nearby particles.

## Still Missing / Partial PixelFlow CFD

Fluid examples still to implement or revisit:

- `Fluid_VerletParticleCollisionSystem`: WIP, not correct yet.
- `Fluid_StreamLines`: full PixelFlow streamline particle rendering, not just current LIC overlay.
- `Fluid_StreamLines_CustomRender`.
- `Fluid_CustomParticles` / Fluid Particles (`https://vimeo.com/184849959`).
- `Fluid_VelocityEncoding`.
- `Fluid_MultipleFluids`.
- `Fluid_GetStarted_TexDataTransfer1/2/3`.
- Any texture transfer and velocity encoding demos should be compared against both code and Vimeo screenshots before implementation.

## Still Missing / Partial PixelFlow Softbody

Softbody examples still to implement:

- `SoftBody2D_Playground`.
- `SoftBody2D_Liquid`.
- `SoftBody2D_ParticleCollisionSystem`.
- `SoftBody2D_Chain`.
- Differential growth examples.
- SoftBody3D cloth, particle collision, and playground.

Do not use `tcxTraerPhysics` for these PixelFlow Softbody examples. The user clarified `tcxTraerPhysics` is independent and unrelated.

## Still Missing / Partial Flow Particles

Remaining particle/flow-field parity targets:

- Cohesion.
- Dam-break style.
- Sprite trails.
- Optical-flow capture particles.
- PixelFlow `FlowFieldParticles_*` examples beyond the first-pass variants.

## Still Missing / Partial ofxFlowTools Helper Parity

Tracked helper/extension gaps:

- Velocity dots / field visualizers.
- `AverageFlowWatcher`.
- Full split-velocity shader graph parity.
- Colorize luminance/velocity/gradient.
- Decay.
- Dilate / erode.
- Inverse warp.
- Normalization.
- Ease.
- Time blur.

Implement these incrementally: one helper family, one small API, one example/test, then docs.

## Broader PixelFlow Scope Still Open

User explicitly said PixelFlow Softbody Dynamics and Computational Fluid Dynamics must be implemented. Broader PixelFlow families should stay in tracked scope unless later split into separate addons:

- Skylight.
- PostProcessing Filters.
- AntiAliasing.
- Shadertoy-style wrappers.
- Sampling.
- Geometry/util examples.

## Build Notes

Xcode/SDK was updated. Some old build caches referenced missing `MacOSX26.4.sdk`. Current working SDK is `MacOSX26.5.sdk`.

When rebuilding old examples, pass the current SDK if CMake cache errors occur:

```bash
SDKROOT_PATH="$(xcrun --sdk macosx --show-sdk-path)"
cmake -S <example-dir> -B <example-dir>/build-macos -DCMAKE_OSX_SYSROOT="$SDKROOT_PATH"
cmake --build <example-dir>/build-macos --parallel 2
```

Examples built for user manual inspection and intentionally left in local `bin` folders:

- `example-lic-streamlines`
- `example-fluid-liquid-text`
- `example-particle-variants`
- `example-split-velocity`
- `example-fluid-verlet-collision`

Known warning remains:

- duplicate `libTrussC.a` linker warning in example builds.

## Git / Commit Caution

- Do not commit generated example `build-macos` / `bin` directories.
- Do not commit `/tmp/tcxFlowTools-reference-captures`.
- `.DS_Store` files are local artifacts and should not be staged.
- `tcxTraerPhysics` was previously treated as a separate independent addon; the user later said `.gitignore` / `tcxTraerPhysics` also need local and remote submission, but this current handoff focuses on tcxFlowTools. Verify current git status before any future final commit.

## Resume Checklist

On resume:

```bash
cd /Users/mac/Desktop/TrussC
git status --short --branch
git log --oneline -5
```

Then continue fluid work first:

1. Re-open `example-fluid-verlet-collision` WIP and compare against `/tmp/tcxFlowTools-reference-captures/pixelflow/verlet-collision/frame-004.png`.
2. Fix mouse fluid injection using a stroke/FBO texture path instead of circular density splats.
3. Rework particle physics toward PixelFlow's CPU Verlet collision behavior.
4. After user accepts this example, update `README.md`, `PHASE_GATES.md`, `REFERENCE_GAPS.md`, and `KNOWN_LIMITATIONS.md`.
5. Continue PixelFlow CFD in this likely order:
   - `Fluid_StreamLines` full streamline particle rendering.
   - `Fluid_CustomParticles` / Fluid Particles.
   - `Fluid_VelocityEncoding`.
   - `Fluid_MultipleFluids`.
   - Texture transfer examples.
6. Keep extracting screenshots from the two Vimeo reference sources before each implementation.
