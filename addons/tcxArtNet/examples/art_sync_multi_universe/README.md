# art_sync_multi_universe

Sends two ArtDmx universes followed by ArtSync.

Expected output:

```text
sent 2 universes and ArtSync
```

Network setup note: set the node IP and directed broadcast IP for your lighting network.

Wireshark filter: `udp.port == 6454`
