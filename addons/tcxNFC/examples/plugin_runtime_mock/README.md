# tcxNFC Plugin Runtime Mock

Demonstrates activation runtime and plugin dispatch without hardware.

```bash
cmake -S . -B build-macos
cmake --build build-macos
./build-macos/tcxNFC_plugin_runtime_mock
```
