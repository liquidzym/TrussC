# trussc_activation_mock

TrussC project-style example that loads `tcxNFC` through `addons.make` and runs a mock activation through the plugin runtime.

```bash
trusscli update -p .
trusscli build
./bin/trussc_activation_mock.app/Contents/MacOS/trussc_activation_mock
```
