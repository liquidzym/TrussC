# tcxNFC NDEF CLI

Builds the exact Type 2 Tag HTTPS URI TLV bytes that will be written to an NTAG.

```bash
cmake -S . -B build-macos
cmake --build build-macos
./build-macos/tcxNFC_ndef_cli https://wstree.cn/t/ABC123
```
