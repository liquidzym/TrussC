# ios-location-notification

TrussC iOS example for `tcxIOS` Location, NetworkStatus, Web/Safari, and local notifications.

For real device testing, add the app privacy keys before requesting permissions:

- `NSLocationWhenInUseUsageDescription`

Local notifications also require the user authorization prompt at runtime.
