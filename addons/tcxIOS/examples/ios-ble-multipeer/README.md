# ios-ble-multipeer

TrussC iOS example for `tcxIOS` Bluetooth LE and MultipeerConnectivity.

For real device testing, add `NSBluetoothAlwaysUsageDescription` to Info.plist. Multipeer discovery requires at least two nearby devices or simulators on the same network.
