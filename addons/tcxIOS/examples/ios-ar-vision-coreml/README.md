# ios-ar-vision-coreml

TrussC iOS example for `tcxIOS` ARKit, Vision, and CoreML bridges.

ARKit requires an AR-capable real device for meaningful validation. Vision and CoreML require real image/model paths.
