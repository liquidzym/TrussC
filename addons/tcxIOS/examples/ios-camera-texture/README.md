# ios-camera-texture

Exercises the camera facade, permission request, frame polling, and texture
upload path. Final camera validation still requires a physical iPhone or iPad.

Keyboard smoke actions on desktop:

- `C`: start/stop camera
- `H`: request haptic feedback

On non-iOS platforms the camera start request should return an explicit
unavailable error.
