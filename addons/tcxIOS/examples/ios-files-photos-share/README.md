# ios-files-photos-share

Exercises the file picker, photo picker, and share sheet facade.

Keyboard smoke actions on desktop:

- `I`: import files
- `P`: pick photos
- `S`: share text
- `E`: export a temporary text file path request

On non-iOS platforms the actions should return explicit unavailable errors.
