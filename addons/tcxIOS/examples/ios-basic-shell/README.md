# ios-basic-shell

Shows the tcxIOS app, device, screen, safe-area, and scene facade.

Keyboard smoke actions on desktop:

- `A`: request a native alert
- `U`: request opening `https://trussc.org`

On non-iOS platforms the actions should return explicit unavailable errors.
